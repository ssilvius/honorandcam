---
import { getEntry } from 'astro:content';
import Layout from '../layouts/Layout.astro';

const homeContent = await getEntry('pages', 'home');
const { Content } = await homeContent.render();
---

<Layout title="Honor & Cam's Wedding">
  <Content />
</Layout>
